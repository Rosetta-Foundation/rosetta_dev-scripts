Background-watch one or more live-verify PRs: auto-dispatch Deploy Organization (or configured workflow) on each new head SHA, wake when the deploy finishes so a human can re-smoke before Approve/merge. Follow the `deploy-verify-watch` skill: arm `.cursor/skills/deploy-verify-watch/scripts/watch-deploy-verify.sh` with `notify_on_output` on `^AGENT_LOOP_WAKE_deploy_verify`. Prefer `--dispatch-on-arm --kickoff`. Pair with `/watch-pr-approve`.