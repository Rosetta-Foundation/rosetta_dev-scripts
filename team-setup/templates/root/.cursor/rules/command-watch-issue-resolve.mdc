---
description: "Background-watch one or more GitHub issues toward resolution (Done-when → close). Follow the `issue-resolve-watch` skill. Chat notify is best-effort — drain watcher-terminal wakes if chat stays quiet."
alwaysApply: false
---

# /watch-issue-resolve (Cursor)

Background-watch one or more GitHub issues toward resolution (Done-when → close). Follow the `issue-resolve-watch` skill: arm `scripts/watch-issue-resolve.sh` with `notify_on_output` on `^AGENT_LOOP_WAKE_issue_resolve`. Use `--kickoff` when work should start immediately. Watched issues are not parking lots.

Chat `notify_on_output` is best-effort — drain `AGENT_LOOP_WAKE_issue_resolve` from the watcher terminal if the chat stays quiet.
